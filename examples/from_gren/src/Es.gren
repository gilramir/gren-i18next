module Es exposing (es_translations)

import I18Next exposing (string, object, fromTree)

es_translations =
    fromTree
        [
            { label = "greeting"
            , item = object
                [ { label = "hello", item = string "Hola" } ]
            }

            ,
            { label = "menu"
            , item = object
                [ { label = "language", item = string "Idioma" } ]
            }
        ]
