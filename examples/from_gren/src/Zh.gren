module Zh exposing (zh_translations)

import I18Next exposing (string, object, fromTree)

zh_translations =
    fromTree
        [
            { label = "greeting"
            , item = object
                [ { label = "hello", item = string "你好" } ]
            }

            ,
            { label = "menu"
            , item = object
                [ { label = "language", item = string "语言" } ]
            }
        ]
