module Th exposing (th_translations)

import I18Next exposing (string, object, fromTree)

th_translations =
    fromTree
        [
            { label = "greeting"
            , item = object
                [ { label = "hello", item = string "สวัสดี" } ]
            }

            ,
            { label = "menu"
            , item = object
                [ { label = "language", item = string "ภาษา" } ]
            }
        ]
