module De exposing (de_translations)

import I18Next exposing (string, object, fromTree)

de_translations =
    fromTree
        [
            { label = "greeting"
            , item = object
                [ { label = "hello", item = string "Hallo" } ]
            }

            ,
            { label = "menu"
            , item = object
                [ { label = "language", item = string "Sprache" } ]
            }
        ]
